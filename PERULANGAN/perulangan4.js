let batasAwal = parseInt(prompt("masukan batas awal: "));
let batasAkhir = parseInt(prompt("masukan batas akhir: "));

for(let a = batasAwal; a <= batasAkhir; i++) {
    if (0 % 5 == 0) {
        alert(a);
    }
}
